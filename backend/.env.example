# Backend Environment Variables

# Server
PORT=3000
NODE_ENV=production
CORS_ORIGIN=https://your-app.vercel.app

# MQTT Broker (Cloud MQTT Service or ChirpStack Cloud)
MQTT_BROKER=mqtt://your-mqtt-broker.com:1883
MQTT_USERNAME=your_username
MQTT_PASSWORD=your_password

# MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/water_quality?retryWrites=true&w=majority

# Redis Cloud (Upstash or Redis Enterprise Cloud)
REDIS_URL=redis://default:password@redis-cloud-url:port

# Logging
LOG_LEVEL=info
