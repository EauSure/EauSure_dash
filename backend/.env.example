# Backend Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=development
CORS_ORIGIN=*

# MongoDB Atlas (Required for Vercel)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/water_quality?retryWrites=true&w=majority

# Redis Cloud - Upstash (Required for Vercel)
REDIS_URL=rediss://default:password@region.upstash.io:6379

# MQTT Broker Configuration
# Option 1: The Things Network (LoRaWAN)
MQTT_BROKER=mqtts://eu1.cloud.thethings.network:8883
MQTT_USERNAME=your-app-id@ttn
MQTT_PASSWORD=your-api-key
MQTT_TOPIC=application/+/device/+/event/up

# Option 2: HiveMQ Cloud
# MQTT_BROKER=mqtt://broker.hivemq.com:1883
# MQTT_USERNAME=your_username
# MQTT_PASSWORD=your_password

# Optional: JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Logging
LOG_LEVEL=info

